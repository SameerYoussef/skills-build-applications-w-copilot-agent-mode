# Octofit Tracker presentation tier environment variables.
# Copy this file to `.env.local` and set your codespace name.
# VITE_CODESPACE_NAME is REQUIRED for API calls to reach the backend on port 8000.
VITE_CODESPACE_NAME=your-codespace-name
