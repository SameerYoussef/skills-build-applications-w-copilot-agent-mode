PORT=8000
MONGODB_URI=mongodb://localhost:27017/octofit_db
NODE_ENV=development

# Codespaces Configuration (automatically set by GitHub Codespaces)
# CODESPACE_NAME is used to build the public API URL:
# https://$CODESPACE_NAME-8000.app.github.dev
# When CODESPACE_NAME is set, the API uses the Codespaces public URL
# When not set, the API defaults to http://localhost:8000
